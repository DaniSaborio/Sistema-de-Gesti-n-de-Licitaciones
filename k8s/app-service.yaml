apiVersion: v1
kind: Service
metadata:
  name: licitaciones-app
  namespace: licitaciones
spec:
  type: ClusterIP
  selector:
    app: licitaciones-app
  ports:
    - port: 80
      targetPort: 8080
      name: http
