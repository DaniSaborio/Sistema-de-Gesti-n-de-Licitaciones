apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: licitaciones-postgres-pvc
  namespace: licitaciones
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
